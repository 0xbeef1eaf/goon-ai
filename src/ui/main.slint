// Main UI file that imports all window components
import { PromptWindow } from "prompt/prompt.slint";
import { ImageWindow } from "image/image.slint";
import { VideoWindow } from "video/video.slint";

export { PromptWindow, ImageWindow, VideoWindow }
